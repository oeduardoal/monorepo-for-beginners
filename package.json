{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">= 8.0.0"
  },
  "scripts": {
    "publish-canary": "HUSKY=0 lerna publish --yes prerelease --preid canary --force-publish --create-release github",
    "publish-stable": "HUSKY=0 lerna publish --yes --force-publish --conventional-commits --create-release github"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.2.0",
    "@commitlint/config-conventional": "^8.2.0",
    "lerna": "^3.20.2"
  },
  "version": "0.0.0"
}
